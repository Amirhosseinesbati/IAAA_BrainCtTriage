# =============================================================================
# IAAA 2026 Brain CT Triage — Submission dependencies
# =============================================================================
# The leaderboard server already pre-installs ALL of these packages (Python
# 3.12) — see the environment manifest. Nothing is installed at evaluation
# time, so this file is informational: it lists every import used by
# submission.py / model.py / triage.py.
#
# NOTE: this submission ships the MONAI + YOLO + MLS-heatmap strategy only.
# It does NOT require nnunetv2 (which is not installed on the server).

pydicom>=3.0.1
pyyaml>=6.0.3
numpy>=2.0
pandas>=2.0
click>=8.1
torch>=2.10
timm>=1.0.22
monai>=1.5.1
ultralytics>=8.3.240
opencv-python>=4.12.0
nibabel>=5.3.3
pillow>=12.0.0
# torchvision is a transitive dependency of ultralytics on the server and is
# only used by model.py as a last-resort fallback if timm is unavailable.
